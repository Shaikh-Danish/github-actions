name: Deploy to GKE

on:
  push:
    branches:
      - main

jobs:
  say_hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print message
        run: echo "🎉 Hello, GitHub Actions!"